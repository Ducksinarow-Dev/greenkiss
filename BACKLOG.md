# The Green Kiss — Dev Backlog

Development backlog for the greenkiss ops tool itself (features, bugs). Numbered items, roughly in priority order within each section. Completed items get struck through and moved to the bottom with the date shipped.

## In progress / just fixed

- ~~#20: Image Repository page + task subtask click-away~~ — **shipped 2026-07-17**. New standalone **Image Repository** section (`src/components/ImageRepository.jsx`, wired in App.jsx, nav in Sidebar.jsx) recreating team.thegreenkiss.com: an alphabetical A–Z directory of vendor image-library links. **Nav:** listed directly under Forms; **Operations Playbook moved to the very bottom** with a `{divider:true}` (space) above it. **Data:** one `imagerepo` kv doc `{blocks:[…]}` — ZERO backend changes (generic kv path, same as the playbook); `getImageRepo`/`saveImageRepo`/`seedImageRepoIfEmpty`/`letterOf` in globals.js. Seeded once with the real 49-vendor list migrated from the live site (Dropbox/Drive/vendor-portal links; multi-gallery vendors become "Vendor — Name" sub-rows under the same letter). **Blocks** are only brand **title-with-link** or free **text** notes, each carrying a `letter`; `letterOf` defaults an unset letter to the title's first char (non-A–Z → "#" group). **Viewer:** sticky floating A–Z menu (`position:sticky top:0`) that floats over the list on scroll — letters with entries render bold/dark, empty ones faded; clicking a letter **filters** to only that letter's brands; a **return icon** (`restart_alt`) restores the full scrollable list; a **return-to-top** button (fixed, bottom-right) appears past 400px scroll; empty letters show a faded capital + "Currently no brands under this letter." **Edit mode** (canEdit only): flat block rows, each with the page-specific **letter dropdown** (A–Z + #) defaulting via `letterOf`, brand-name + link inputs (or a textarea for notes), delete, and Add brand / Add note. **Task Manager fix (`TaskManager.jsx`):** the column-card "add subtask" input now has `onBlur={submitAddSub}` — clicking away saves what was typed or discards a blank row (previously only Enter/Escape/Add did anything). Verified end-to-end in the dev browser (light + dark, console clean): nav order + moved playbook; seeded A–Z with faded empty letters; letter filter; empty-letter note; return-icon show-all; sticky menu on scroll; return-to-top button; edit mode with letter dropdown defaulting to "Z" from "Zephyr Beauty" typed live; delete round-trip; and both subtask click-away paths (typed → saved to `subTasks`, blank → nothing added) confirmed against localStorage.
- ~~#19: Round 4 — forms become real forms (submissions + browse), nav reorder, Task Manager upgrades, dashboard reorder, editor sizing~~ — **shipped 2026-07-17**. **Nav (Sidebar.jsx):** order is now Dashboard ‖ Tasks/Projects/Calendar ‖ Library/Forms/Playbook with two thin dividers (`{divider:true}` entries in NAV_ITEMS); Admin Panel is pinned at the sidebar bottom in its own bordered block (admins only); extracted `NavButton`. **Form identity (Phase B):** `defSOP` skips the auto-index block for forms; `SOPEditor` is kind-aware (`New Form`/`Editing Form`, "Form title…", form-flavored duplicate/delete copy); forms hide the Index add-block entry and the per-block Task-role dropdown. List blocks gained a **"Plain" style** (Plain/Bulleted/Numbered segmented control) — plain + entry-field renders as **bold label + ":" + underlined blank**, the FRM-001 slot shape, in viewer, fill mode, and editor. **Form submissions (Phase C — the instances revival):** the retired-in-#17 `instances` plumbing is back as form submissions with ZERO backend changes (`instance_save`/`instance_delete` existed since v0.1.9). `newSubmission(sop,user)` snapshots blocks + `values/notes/editLog`; the form viewer header shows **Fill out** + **Submissions (n)** instead of Run SOP; new `FormFill` renders from `blocksSnapshot` (template edits never rewrite records) — list checks/entries and completion write into `values`, every block has a subtle per-block **Add note** (pre- and post-submit), Submit auto-fills completion blocks (user + today) and sets `status:"submitted"`; the record stays editable but each post-submit save appends a **read-only "Edited by X · date-time" log** (`stampEditLog`, deduped per user+minute). `SubmissionsSlideOver` lists a form's own records. **View filled forms (Phase C+, SOPLibrary.jsx):** a browse toggle on the Forms page — `MiniCalendar` (exported from TaskManager, given a `dots` prop) with per-form colored date dots (`formColor` stable hash → CATEGORY_COLORS) + a legend; clicking a date lists that day's submissions on the right; "Show all forms (n)" flat newest-first list; every row reopens its submission via the viewer's new `openSubmissionId`. **Task Manager (Phase D):** Board/**List** toggle persisted per-browser (`localStorage["gkTasksView"]`, mirroring Projects) — list = vertical sections per status, same cards in a fluid grid, still live drop targets; tasks gained **`links:[{id,label,url}]`** — a Links section in the modal (label input + the shared magnet-aware `LinkPopover`) with clickable previews, and cards render them via `ItemLink` (moved from SOPViewer into shared.jsx to avoid an import cycle; magnet links navigate internally via a nav object threaded from App); modal width 560→**680** (SOP-run stays 720); **Tags moved onto the Type row** (far right); **Send alert** button in the modal footer (user-picker popover, reuses `addAlert`, inline "Alerted X" confirmation); **Hide project tasks** filter toggle; **project grouping** — within each column/section tasks cluster by project (stable sort by project name after `sortTasksForUser`) under a slim project-colored name label. **Login reminders (LoginReminders.jsx):** third bubble type — one toast per alert flagged at me (task title, colored priority flag when high/urgent, due date, "Flagged by X"); the whole toast opens the task via a new `onOpenTask` (App's `goToTask`) and clicking OR dismissing deletes the alert record, keeping the dashboard strip in sync. **Dashboard (Phase E, MyDashboard.jsx):** sections are now Alerts strip → **Today's Tasks** (overdue + due today; red-accented only when something is actually overdue) → **Assigned Tasks** (this week + later/no-date) → My Projects → **My Forms** (in-progress submissions I started; row = form title + started date; click deep-links into fill mode via a new `focusSubId` prop on SOPLibrary + `goToSubmission` in App, mirroring `focusBlockId`). **Editor sizing (Phase F):** editor + viewer maxWidth 820→**1000**; block-toolbar selects 40→56px (no more truncated "100%/None/Desc"); list-editor header restructured into two stacked containers (style control + convert-to-text LEFT, both toggles RIGHT); **Italics** button added to the WYSIWYG toolbar. Also: vite dev server honors an assigned `PORT` (launch.json `autoPort`) so previews don't fight other dev servers over 5173. Verified end-to-end in the dev browser (both themes, console clean): nav order/dividers/pinned admin; New Form identity (no index, no task dropdowns, Plain style present); FRM-001 slot shape; full fill→note→submit→auto-completion→post-submit edit→editLog cycle with localStorage confirming `values/notes/editLog`; dashboard My Forms deep-link back into the same fill; calendar dot on the right day + day list + show-all + reopen; submissions slide-over; task list view + hide-project-tasks + project cluster labels; modal at 680 with Tags beside Type; magnet link inserted from internal search on a task, rendered on the card, click navigated to the right SOP; Send alert → logged in as Megan → alert toast with "Flagged by Hayden" → click opened the task modal AND deleted the alert record; Run SOP gate modal regression intact; 1000px canvas + 56px selects + Italic round-trip. Not driven by hand: drag-drop between list-view sections (same `onDropColumn` handler as board columns).
- ~~#18: SOP round 3 — polish, WYSIWYG, magnet links, Run-SOP routing, Playbook history, board/login QoL~~ — **shipped 2026-07-17**. **Visual polish:** viewer within-block spacing tightened (list gap 7→4, para 1.75/16→1.6/12, heading 26→20 — block gaps untouched); `ListBlockEditor` toggles restructured into one aligned 28px-tall row (the stair-step from Hayden's screenshot); SOP library category pill row replaced with one `<select>` + counts + "Clear filter". **Link visibility (the "I can't see links" fix):** per-item link control is now a bordered, labeled "Link"/"Linked" button (moss when set) inside the block border; item rows flex-wrap so controls can't spill at 33/40% widths; viewer shows a glyph on every linked item. **Magnet links (new):** `gk:sop:<id>[:<blockId>]` / `gk:task:<id>` / `gk:playbook:<sectionId>` plain-string deep links that work in every URL field with zero storage changes — copy buttons in every editor block toolbar, on viewer-block hover ("Copy link"), Playbook page header, and the task modal; one `openMagnet()` resolver rides the existing `onNavigate` plumbing (extended with a `task` kind + `blockId` anchor scroll via the existing `blk-<id>` viewer anchors; TaskManager gained a `focusTaskId` deep-link that opens the task's modal, mirroring `sopFocus`); shared `LinkPopover` (shared.jsx) has Web-address + Internal-magnet tabs, the internal tab searching SOPs/Forms by title+code, their **numbered blocks** as sub-entries, Playbook pages, and Tasks by title/tag (`getLinkSearchCandidates`). **Block features:** `divider` ("Line Break") block; list→text revert (mirror of convert-to-list, prefix-preserving round-trip); **WYSIWYG text blocks** — contentEditable + `document.execCommand` (deprecated-but-universal, zero deps), toolbar = Bold / XB (weight-800 span) / S-M-L / 4 brand color swatches / Link (LinkPopover, magnet-aware) / @-mention insert; stores sanitized `html` + synced plain `text` (innerText) so search/excerpts/taskFromSop needed zero changes; viewer renders html through `sanitizeHtml`+`mentionTokensToHtml` with one click-delegate for mention pills + gk: anchors; legacy text blocks untouched until first edit. **Run-SOP routing (#10/#11):** blocks gain `taskRole` (None/Desc/List dropdown in the toolbar, saved on the SOP); `taskFromSop` rewritten — description = compact auto-summary (title, code, section list) + only Desc-routed blocks, subtasks = only List-routed items; Run SOP with no roles set opens a gate modal ("Not set up for tasks yet") with an Open-editor shortcut, forcing the one-time setup. **Board/login QoL:** empty "Review Before Closing" collapses to a 44px dashed vertical tab that's still a live drop target; new `LoginReminders` (own file) shows once-per-session bottom-right bubbles — overdue/due-today tasks + a separate "N SOP runs for today" bubble — dismissible, click-through to Task Manager. **Playbook history:** `playbookRevs` kv (cap 20, dedup-guarded), snapshots on edit-mode exit / before section delete / before any restore; sections stamp+display "Edited by X · date"; `PlaybookHistoryModal` (HistoryPanel-style two-pane) previews section lists and offers **Restore entire version** or **Restore page** (by-id replace, re-adds if deleted). Generic toast: `triggerToast(msg)` reuses the Saved toast with custom text ("Magnet link copied"). Verified in the dev browser end to end: gate modal → role setup → Run SOP task with summary + 4 subtasks on the board; magnet inserted from internal search, persisted as `gk:sop:…`, click navigated to the right SOP; bold survived edit→save→viewer; legacy checklist auto-migrated to list+checkboxes on edit; 44px review tab; both reminder bubbles; playbook rename → revision with attribution → per-page restore round-trip (TEMP NAME → restored) with the before-restore snapshot deduped correctly; divider persisted + rendered; light/dark toggle clean; zero console errors. Not driven by hand: drag-drop onto the collapsed review tab (same `onDropColumn` handler as the full column) and the WYSIWYG size/color buttons (same `execCommand` path as the verified bold).
- ~~#17: SOP editor round 2 — editing UX, index/numbering, list+checkbox merge, Run SOP → Task~~ — **shipped 2026-07-16**. Batch from Hayden after using v0.1.9. **Bug: text not mouse-selectable in the editor** — `BlockRow`'s outer div had `draggable` set unconditionally; in Chromium a draggable ancestor blocks text selection inside child inputs (hence "must use arrow keys"). Fixed by gating `draggable` on state that's only true while the drag *handle* is held (onMouseDown→true, onDragEnd/onMouseUp→false); verified drag-select works and reorder still works from the handle. **Bug (found during verify): every "due today" task showed "Overdue"** — date-only strings (`"YYYY-MM-DD"`) were parsed via `new Date(str)` as UTC midnight, landing a day early in any timezone behind UTC. Added `parseDate()` in globals (bare dates → local midnight, full ISO untouched) and routed `fmtDate`/`fmtDateShort`/`isOverdue`/`isDueToday`/`isDueThisWeek` through it. **Editing reach**: `BlocksEditor` gained a sticky floating action bar (Add Block + Done, menu opens upward); `SOPViewer` header (Edit / Print / Run SOP) is now `position:sticky` so staff never scroll up. **Version/code beside title**: moved the `typePrefix`/`code` inputs to a right-aligned area on the editor title row; viewer shows the code as a mono chip to the right of the `<h1>`. **List/Checklist merge (#11)**: one `list` block with toggles — bulleted/numbered · "Add checkboxes to beginning of each line" · "Show entry field at end of each line". Legacy `checklist` blocks normalize to a checkbox list on read via `asListBlock()` (shared by editor/viewer/`taskFromSop`), so old data renders and re-saves as a list; `checklist` dropped from the Add-block menu. **List-item links (#5)**: each item has an optional `url` (compact link popover in the editor, rendered as an `<a>` in the viewer). **Convert-to-list (#2)**: a text block whose lines all look like list items (`•/-/*`/`N.`) shows a "Convert to list ▾" affordance (bulleted / numbered / checklist), stripping the leading marker. **Per-block emphasis background (#7)**: optional `bg` key (none/sage/blush/neutral) resolved theme-aware via `blockBg()` in globals; picker in the block toolbar. **Numbering + Index (#9/#10)**: blocks gain optional `num` (editable `#` field in the toolbar); new `index` block auto-inserted at the top of every new SOP (`defSOP`), rendering a TOC from headings + numbered blocks with jump links (`scrollIntoView` to `#blk-<id>`). **Interactive checkboxes in-doc (#4) + instance retirement**: the Phase-2 "Today's Run"/Past-Runs/Completion-lock in-viewer instance model is **retired** (Hayden couldn't locate it; execution moved to Tasks). Checkbox + entry-field state is now component-local session state (ticking works directly while reading, resets on reload). Completion block is a plain session-fillable/printable footer (no lock). The `instances` kv collection + api.php actions are left in place, unused (`// ponytail: retired`), to avoid backend churn. **Run SOP → Task (#12)**: new `taskFromSop()` builds a draft task (title `"<SOP> — <today>"`, description from text/heading/plain-list content, checkbox items → subtasks, `relatedSopId`, assignee=current user, due=today, `fromSopRun:true`); the viewer's "Run SOP" button opens the existing `TaskModal` (exported from TaskManager) in a **wider** layout (`maxWidth` 720 via a `wide` prop), prefilled, requiring an explicit Create. Day-end "Complete run" — an inline notes panel on `fromSopRun` tasks (replaced a `window.prompt` with an in-modal field for testability + better UX) records `runCompletion:{by,at,notes}` and marks the task done. Forms use the same button labeled "Fill out." Verified end-to-end in the dev browser: selectability, convert-to-list, list toggles + item link, index + numbered block, sage background (light+dark), floating headers, in-doc checkbox ticking, full Run→Create→board→Complete-run cycle (localStorage confirmed `status:done` + `runCompletion`), the date fix (task due today no longer overdue), and the legacy checklist SOP rendering + flowing to Run→Task. The only console errors seen were HMR fast-refresh artifacts (`emptyForm` non-component export) during live edits — gone on clean load. Not verified: real .docx upload (browser tool can't drive a native file picker).
- ~~#16: Auto-reload after a software update~~ — **shipped 2026-07-16**. cPanel copies the new build into `public_html` asynchronously, so `admin_deploy` returns before `VERSION` on disk actually flips — the admin was left on the stale bundle until they reloaded by hand. `DeployPanel` (`src/components/AdminPanel.jsx`) now, after a successful deploy, polls `version_info` every 3s and `window.location.reload()`s as soon as the reported version matches the pending release (`ghVersion`) — or, if the GitHub check failed, as soon as version/commit changes from the pre-deploy value. Bounded at 120s: a stuck/failed deploy falls back to a "give it a minute and reload" message + the cPanel manual path. While polling, the button is disabled and an inline "Update in progress — this page will reload automatically…" row shows with a "Reload now" manual escape hatch. Panel is REMOTE_MODE-only (no cPanel in dev), so verified by build/lint + logic review, same as #13/#15's deploy pieces. Note: takes effect from the update AFTER the one that installs it — the currently-live build has no auto-reload code, so that first Update Now still needs one manual reload.
- ~~#15: SOP system overhaul — resizable blocks, new block types, daily fill-out runs, Playbook, Forms, internal linking~~ — **shipped 2026-07-16**. Big one, driven by Hayden's real ops docs. **Block system**: every block now carries an optional `width` (33/40/50/60/100%, default 100) laid out with native flex-wrap (no packing code) — width picker lives in each block's icon column in `SOPEditor`; `SOPViewer` mirrors the wrap. New block types in `BLOCK_DEFS`/`newBlock()`: `list` (one flexible type covering both plain and numbered/bulleted, with an optional trailing "entry" blank per line for `Orders waiting to ship: ___` style fields), `completion` (fixed Completed By/Date/Notes footer), plus `heading` gained an optional `description` sub-field ("Title & Description"). Heading text auto-suggests previously-used headlines across all SOPs/Forms via a native `<datalist>` (`getAllHeadingTexts()`). Checklist checked-items got the faded-strikeout treatment. **Daily fill-out "Instances"** (`instances` kv collection, `instance_save`/`instance_delete` in api.php, same collision-safe `collectionUpsert` pattern as tags): any doc with a checklist / list-with-entry / completion block (`hasFillableBlocks()`) shows a "Today's Run" strip in `SOPViewer` — Start/Continue/Completed states + a "Past Runs" history slide-over. A run snapshots the template's blocks at start time (`blocksSnapshot`) so later template edits never rewrite history; checklist/entry/notes state writes into `instance.values`; the Completion block auto-fills Completed By (session user) + Date (today) and locks the whole run read-only on submit. **Internal linking** (Phase 3): inline `@[Label](kind:id)` tokens (kind ∈ sop/form/contact/playbook), inserted via a shared `MentionField` (@-trigger popover, `getMentionCandidates`) and rendered as clickable pills by `MentionText` (`parseMentionText`); contact pills pop an info card, doc/playbook pills navigate cross-section via App.jsx's `goToSop`/`onNavigateOut`. "Referenced by" backlinks computed lazily at render (`findBacklinks`, no reverse index). New `contacts` collection (`contact_save`/`contact_delete`). **Hierarchy** (Phase 4): one-click "Load standard sections" seeds the 12 numbered Green Kiss sections as categories (`seedStandardSections()`, idempotent, name-collision-safe, never auto-runs); optional free-text `typePrefix`/`code` fields on each SOP (SOP-OPS-001 style), shown as a chip only when set, so old SOPs look unchanged. **Operations Playbook** (new standalone nav item, `src/components/OperationsPlaybook.jsx`): one `playbook` kv doc `{sections:[{id,title,blocks}]}`, seeded once from the real playbook doc (`seedPlaybookIfEmpty`), split into sub-pages with a section switcher + drag-reorder; each sub-page edits with the SAME block system (extracted `BlocksEditor` from SOPEditor) and renders as a formatted document (`ViewerBlock` exported from SOPViewer). Key Contacts is a live `contacts` view with contact info beside each name. **Forms** (new standalone nav item): `SOPLibrary` parametrized by a `kind` prop ("sop"|"form") — Forms reuses the entire library/editor/viewer/instances stack, just filtered + relabeled; `defSOP(categoryId, kind)`. **Importer fix** (Phase 7, `parseDocxHtmlToBlocks`): consecutive plain `<p>` now merge into ONE text block (flushed on a heading/list/image break) instead of one-block-per-paragraph, and a whole-paragraph-bold `<p>` (single `<strong>`/`<b>` child) is detected as a heading — the concrete fix for Pages-exported docs that carry no Word heading styles, without raw-XML font-size parsing. Verified live in the dev browser end to end (all block types + widths in light/dark, mention insert+navigate, full Start→check→complete→lock→history run, section seeding, Playbook sub-pages + contact + mention resolution, Forms section); importer merge/heading logic separately verified against representative mammoth-style HTML (10 source elements → 8 clean blocks). **Two real data-loss bugs found and fixed during verification**: (1) a pre-existing stale-closure in SOPEditor's unmount-persist effect was silently reverting every edit on close — the mount-time `persist` closed over the SOP's original state; fixed by routing the cleanup through a `persistRef` updated each render. (2) instance dates used UTC (`toISOString().slice(0,10)`), tagging evening-in-North-America runs with tomorrow's date and breaking "today's run" detection; added `todayLocalISO()` and switched today-lookup to `getTodayInstance` (matches in-progress OR completed, so the strip survives reload). Not verified: real .docx upload through the file picker (browser tool can't drive a native picker) — worth a manual FRM-001 import test.
- ~~#5: SOP importer from PDF/Word~~ — **shipped 2026-07-16**. New "Import" button next to "New SOP" in SOPLibrary (editor/admin only, `src/components/SOPImporter.jsx`), accepting .docx/.pdf. Parser libraries are NOT in the main bundle — mammoth.js (`mammoth@1.8.0`) and pdf.js (`pdfjs-dist@3.11.174`, legacy build) load on demand from a pinned jsDelivr CDN URL via plain `<script>` injection on first import only. DOCX: mammoth converts to HTML, then h1-h4 → heading blocks, paragraphs → text blocks, consecutive `<li>` within one list → one text block ("• "-joined lines), `<img>` → image blocks (mammoth's default base64 data URIs get piped through the SAME `processAndStoreImage()` every other image upload uses, so REMOTE_MODE uploads for real and dev mode just re-encodes — no separate image path). Links inside paragraphs flatten to plain text in v1 (`// ponytail` noted in-file). PDF: pdf.js extracts per-page text runs, groups them into lines by y-coordinate, then a heuristic (short line, no trailing sentence punctuation, followed by a longer line → heading; everything else merges into blank-line-separated paragraph blocks) builds the same Block[] shape. No image extraction from PDFs in v1 (`// ponytail` noted). Either path lands the user straight in the existing SOPEditor with a new draft pre-filled (title from filename, parsed blocks) — no new preview UI, the editor IS the preview. Unparseable/unsupported files show an inline error, nothing gets created. Verified in the dev-mode browser: hand-built test .docx (zipfile-crafted, real mammoth conversion) and a hand-written test .pdf (manual PDF syntax, real pdf.js extraction) both imported correctly — headings/text landed as expected, saved, and showed in the library. The li-grouping branch was separately sanity-checked against real `<ul><li>` HTML since the test docx's numbered paragraphs needed a `numbering.xml` this fixture didn't include. Found and flagged (not fixed, out of scope) a pre-existing duplicate-save bug in SOPEditor's autosave/unmount persist() calls — see spawned task.
- ~~#13: Rollback to previous release~~ — **shipped 2026-07-16**. No git surgery — cPanel's deploy API can only redeploy the checked-out `release` branch HEAD, so rollback restores files from local snapshots instead. Before every `admin_deploy`, api.php now snapshots the CURRENTLY deployed build (`index.html`, `assets/`, `VERSION` — not `config.php`/`uploads`/`backups`/`api.php` itself) into `gk_releases/<version>-<commit>/`, next to the backups dir with the same deny-all `.htaccess` protection, keeping the newest 5. New admin-only actions `release_list` (reads snapshot dirs) and `release_rollback {name}` (validates `name` against the actual directory listing — no path traversal possible — snapshots the CURRENT build first so the rollback is itself undoable, then copies the chosen snapshot's files back over the live build, and records a `rollback:true` flag on `lastDeploy`). Admin Panel's Software Update card gained a collapsed "Previous builds" section (`src/components/AdminPanel.jsx`): version/commit/date per snapshot, a "Roll back" button behind a two-step confirm modal ("This replaces the live build with vX.Y.Z. Data is not affected... snapshotted first so this can be undone too"), and the "Last deployed" line now reads "Rolled back to vX.Y.Z by NAME, DATE" when the flag is set. Shows "Snapshots appear after your next deploy" until the first post-#13 deploy runs. `api.php itself is NOT snapshotted` (`// ponytail` noted — a bad api.php change is fixed by deploying forward, not rolling back, until that ever proves insufficient). Verified: `php -l` clean; wrote a standalone PHP CLI test (extracting the pure-filesystem snapshot/list/rollback functions and running them against temp dirs, no PDO needed) covering no-op-before-first-deploy, correct naming, idempotent re-snapshot, rollback restoring prior content, path-traversal rejection, and 5-deep retention — all pass. UI verified in the dev-mode browser: the whole Software Update/Previous-builds card correctly stays hidden in dev mode (REMOTE_MODE false), matching the existing Backups-panel precedent (no cPanel to deploy or roll back locally).

- ~~#2: Light/dark mode toggle~~ — **shipped 2026-07-16**. The C_LIGHT/C_DARK palettes, `setTheme()`/`getTheme()`, and the dark CSS overrides already existed in globals.js/global.css from earlier work — what was missing was the actual toggle. Added an icon button (light_mode/dark_mode) in the Sidebar next to Log out, available to every role. App.jsx holds a `themeVersion` counter bumped on toggle; since no component here is memoized, that one state change cascades a full re-render through the tree and every inline style re-reads the freshly-mutated `C` object — no remount needed. Persists via the existing `localStorage["gk_theme"]` key. Fixed two light-only spots found during the sweep: the MyDashboard empty-state logo (added the same invert+screen dark variant Sidebar already used) and the SOPLibrary category count chip (`rgba(255,255,255,0.6)` → `C.sur`). Verified in both themes across Dashboard, SOP Library/Viewer/Editor, Task Manager (board + Done slide-over + task modal), Projects (board + list), Content Calendar (calendar + item editor), Admin Panel, and the PIN modal — plus persistence across reload. Print stylesheet was already forced to black-on-white regardless of theme; left untouched.
- ~~#3: Typography pass~~ — **shipped 2026-07-16**. Body/UI now runs on Manrope (loaded in index.html alongside Jost, weights trimmed to what's actually used: Jost 400/500/600, Manrope 400-800). Jost is kept only for the uppercase letterspaced treatment via a new `FONT_CAPS` constant in globals.js, centralized in shared.jsx (`Btn`, `OBtn`, `Pill`, `lbl()`, `SectionHeader`, `SlideOver`) and swept onto every remaining `textTransform:"uppercase"` call site across every component (nav, tabs, pills, section headers, category chips). `inp()` switched from Jost to Manrope since form field text isn't part of the caps treatment. IBM Plex Mono untouched.
- ~~#1: 1Password/LastPass/Dashlane repeatedly prompting to save the PIN as a password~~ — **fixed 2026-07-15**, shipped as v0.1.2. Root cause: browsers deliberately ignore `autocomplete="off"` for password-manager save-prompts; added the vendor-specific `data-1p-ignore`/`data-lpignore`/`data-form-type="other"` opt-outs to every PIN field.
- ~~#6: Task Manager board columns~~ — **shipped 2026-07-16**. Done removed from the main board; new `reassigned`/`review` statuses (labeled Reassigned / Review Before Closing) added between In Progress and Done. Done lives behind a "Done (n)" button that opens a right-hand slide-over (shared `SlideOver` component in shared.jsx, reused by Projects too) — unchecking a card there sends it back to To Do. Project detail's task grouping mirrors the same columns + Done slide-over.
- ~~#7: Task types~~ — **shipped 2026-07-16**. `TASK_TYPES` (task/note/milestone) in globals.js, segmented type picker in the task modal, distinct icon per type on cards (milestones get the icon in brand green + bolder title), notes skip the overdue-red treatment. Untyped legacy tasks normalize to "task" on read via `taskType()` — no migration write.
- ~~#10: Task ↔ Project attach/detach verification~~ — **verified 2026-07-16**, no bugs found. Created a milestone task with 2 subtasks (one assigned + dated), attached to a project, confirmed it appeared in project detail with subtasks intact, detached back to "No project," confirmed subtasks/assignee/date and type all survived and it left the project view. `projectId` being a plain field already made this work correctly — no code changes needed.
- ~~#14: Project board + list view~~ — **shipped 2026-07-16**. Project statuses remapped to `upcoming`/`in_progress`/`approval`/`done` (+ `archived`), normalized on every `getProjects()`/`getProject()` read (`active`→`in_progress`, `on_hold`→`upcoming`) rather than a bulk migration write — old values persist until a project is next saved through `updateProject`/`addProject`. Board view (default) has Upcoming/In Progress/Approval columns with drag-to-change-status; Done + Archived hide behind the same slide-over treatment as #6 (archived projects keep their own "Archived" pill inside it). List view groups the same projects into vertical sections by status, Done+Archived collapsed at the bottom. Board/List toggle persists per-browser via `localStorage["gkProjectsView"]`. Pills updated everywhere projects render (board, list, detail header, My Dashboard cards) via the existing shared `projectStatusMeta`.
- ~~#4: Inline category creation from the SOP editor~~ — **shipped 2026-07-16**. Category `<select>` in SOPEditor gained a "+ New category…" option that opens a small popover (name + the same `CATEGORY_COLORS` swatch set Admin Panel uses); creates via the existing collision-safe `addCategory()` path and selects the new category immediately. Admin Panel remains the only place to rename/recolor/delete.
- ~~#8: Inline task metadata popovers (ClickUp-style)~~ — **shipped 2026-07-16**. Tags are a new foundation collection (`tags` kv key, `tag_save`/`tag_delete` per-record server actions, `task.tagIds`) with a create-on-the-fly flow. Task tile rebuilt to the reference-design anatomy: title, description indicator, four always-visible outline icon buttons (assignee/date/priority/tags), subtask disclosure toggle. Each icon opens a small anchored `Popover` (new shared component — click-outside/ESC/viewport-clamped) editing just that field via the existing collision-safe `updateTask`, no full-modal detour. Due-date popover has a hand-rolled `MiniCalendar` (month grid, no library) plus a Recurring section (none/daily/weekly/monthly persisted as `task.recurrence`); completing a recurring task auto-spawns the next occurrence (`completeTaskWithRecurrence` in globals.js — fresh copy, new id, status todo, dueDate advanced from the current due date, subtask done-flags reset) while the completed one keeps its done state. Subtasks are first-class: the disclosure expands into nested mini-cards with their own three-icon row (assignee/date/priority — no tags in v1) and gained a `priority` field.
- ~~#9: Task tile hover actions~~ — **shipped 2026-07-16**. Hover (or focus/tap, for touch) reveals a floating pill top-right of the tile: Mark Complete, Add Subtask (inline input), Rename (inline edit), and a "⋯" overflow menu (all buttons stop propagation so drag-and-drop is unaffected). Overflow menu: **Favourite** (`task.favouritedBy`, favourited tasks star + sort to the top of their column for that user — `sortTasksForUser` shared by Task Manager and Projects' embedded board); **Alert staff member** (new `alerts` collection, per-record `alert_save`/`alert_delete` — any authenticated user can create, delete requires being the target/creator/admin; My Dashboard gained a rose-accented Alerts strip at the top with a dismiss X); **Duplicate**; **Merge into…** (description appends, subtasks move, tags union, source deleted, confirmed first); **Add to project…**; **Templates** (new `taskTemplates` collection, per-record actions — save/apply/delete, snapshot strips id/status/assignee/dates); **Archive** (`task.archived`, hidden from board + dashboard everywhere; the Done slide-over gained a Done/Archived segmented toggle with Unarchive); **Delete**; **Convert to…** Project (subtasks become real tasks in a new project, original deleted) or Subtask of… (becomes `{text,done,assigneeId,dueDate,priority}` on a target task, original deleted, confirm warns about flattened description/subtasks/tags). Every per-record action dispatches through one shared `dispatchTaskAction()` in globals.js so Task Manager and Projects' embedded board stay in sync. Skipped "Convert to list" — no list concept exists in this app. Verified in both themes (tile, popovers, hover pill, overflow menu, alert strip) and cross-user (Hayden alerts Megan, logged in as Megan to see the dashboard strip).

## Priority queue

### Design / theming
- ~~#2: Light mode switch.~~ — shipped 2026-07-16, see top of file.
- ~~#3: Typography pass~~ — shipped 2026-07-16, see top of file.

### SOP Library
- ~~#4: Add new categories inline from the SOP editor~~ — shipped 2026-07-16, see top of file.
- ~~#5: SOP importer from PDF/Word~~ — shipped 2026-07-16, see top of file.

### Task Manager — overhaul
- ~~#6: Board columns~~ — shipped 2026-07-16, see top of file.
- ~~#7: Task types~~ — shipped 2026-07-16, see top of file.
- ~~#8: Inline task metadata popovers (ClickUp-style)~~ — shipped 2026-07-16, see top of file.
- ~~#9: Task tile hover actions~~ — shipped 2026-07-16, see top of file. "Convert to list" was skipped — no list concept exists in this app.
- ~~#10: Task ↔ Project attach/detach~~ — verified 2026-07-16, see top of file.

### Projects
- ~~#14: Project board columns.~~ — shipped 2026-07-16, see top of file.

### Admin / Ops
- ~~#13: Rollback to previous release~~ — shipped 2026-07-16, see top of file.
- **#22: Admin page 2-column layout** (added Jul 21 2026) — arrange the Admin Panel cards in a two-across grid: **Users** (50%) beside **Software Update** (50%), then **Categories** (50%) beside **Backups** (50%). `src/components/AdminPanel.jsx`.

### Content Calendar
- **#21: Shopify target-sales calculator** — link to Shopify to calculate target sales numbers for day/week/month (added Jul 20 2026 during the Content Calendar expansion). Needs a Shopify custom-app **Admin API access token** (not yet created) created in Shopify admin → Apps → Develop apps when this is picked up; the key must stay server-side (proxy through `api.php`, same pattern as the new `omnisend_*` actions). Out of scope for the expansion pass — that pass covers campaign/email/GBP/metrics/calendar-sync/tools-prompts, with sales metrics entered manually (Omnisend live for email only).

### AI features — DEFERRED (Hayden, Jul 16 2026: revisit after staff have used the tool and real search/automation needs are clearer)
- **#11: AI search agent across the whole app** — natural-language search over SOPs/tasks/projects/content.
- **#12: AI action agent** — natural-language prompts to create/assign/reassign SOPs and tasks, pull from a project or campaign, and generate a presentation-style summary with imagery and links.

## Notes for whoever picks up #11/#12
Both need a decision on: which model/API (cost target — "low cost" was specified), whether calls proxy through `api.php` (keeps the key server-side, required either way) or hit a provider directly from the client (insecure — never expose an API key client-side), and how much scope for v1 (search-only first is a much smaller lift than the full action-agent in #12; recommend shipping #11 before attempting #12).
