node_modules/
dist/
.DS_Store
*.log
.vite/
USER_GUIDE.pdf

# Backend — server-side secrets and runtime data, never committed.
config.php
uploads/
backups/
VERSION

# Claude Code local overrides — machine-specific paths and grants.
.claude/settings.local.json
